# RAG-Anything multimodal RAG integration.
# 'all' pulls image + text + office extras; drop to e.g. raganything[image,text]
# if you don't need every parser. See https://github.com/HKUDS/RAG-Anything
raganything[all]
python-dotenv
# Pre-flight page triage (rag/triage.py) — cheap signal extraction before
# paying for OCR/LLM parsing. Used by `ingest.py --triage` and standalone.
pymupdf>=1.24
