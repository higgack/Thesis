# AGENT GUIDE

이 문서는 이 프로젝트에서 에이전트가 사용자와 소통할 때 지켜야 하는 기본 규칙을 정의합니다.

**우선순위**: 승인·출력·작업완료에 관한 규칙은 `AGENTS.md`가 최종 기준입니다.
이 문서와 어긋나면 `AGENTS.md`를 따르세요. 지시문을 감사할 때 두 파일을 같이 봅니다.

예외는 **§4의 답변 순서 하나뿐**입니다("기본"이지 "항상"이 아니므로, 길이와 각
항목 안의 순서는 `AGENTS.md`의 Token-lean「결론 먼저」가 계속 지배합니다).
**§3의 "요청했을 때만 코드"는 예외가 아닙니다** — VM 명령, 실패한 스크립트의
실제 출력, 승인을 구하는 diff는 요청이 없어도 보여줍니다(`AGENTS.md`의
「VM ops」와 「완료 보고 전 검증」이 요구함).

## 1) 언어 규칙
- 모든 답변은 한국어로 작성합니다.

## 2) 대상 독자 규칙
- 설명 대상은 비개발자입니다.
- 어려운 개발 용어는 쉬운 한국어로 풀어서 설명합니다.

## 3) 설명 우선순위 규칙
- 코드 자체 설명보다 비즈니스 목적, 현재 상태, 문제점, 개선방안, 예상 효과를 우선 설명합니다.
- 사용자가 코드를 요청한 경우에만 코드를 제시합니다.

## 4) 답변 형식 규칙
- 답변은 아래 순서를 기본으로 사용합니다.
  1. 비즈니스 목적
  2. 현재 상태
  3. 문제점
  4. 개선방안
  5. 예상 효과

## 5) 변경 작업 규칙
- 파일 수정은 사전 승인 없이 진행합니다. **트리거 단어가 오기 전까지만** 디스크에
  쌓아둡니다 — 트리거가 오면 커밋→푸시→배포 파이프라인을 끝까지 진행하며, 이 줄을
  근거로 중간에 멈추지 않습니다.
- 승인이 필요한 것은 **커밋·푸시·배포**, 그리고 아래의 **되돌릴 수 없는 작업**입니다.
  커밋 판단 기준은 `AGENTS.md`의 「⛔ Commit/push gate」가 유일한 출처입니다.
- 되돌릴 수 없는 작업은 먼저 확인합니다: 데이터 삭제, `_ENT_STOP`·`_ENTITY_ALIASES`
  추가(엣지 대량 삭제·병합), `.env` 변경 제안, 위키 토픽 삭제. 이 목록은 예시이고
  원본은 `AGENTS.md` 「Standing rules」에 있습니다 — 목록에 없어도 되돌릴 수 없으면
  먼저 묻습니다. **force-push는 여기 없습니다** — 확인받고 하는 게 아니라 금지입니다
  (`AGENTS.md` 「Branch / push policy」).

> 2026-09-06 개정. 원래 문구는 "수정 전에 계획을 먼저 설명하고 사용자
> 승인을 받은 뒤 진행합니다"였습니다. 커밋 게이트와 겹치면 한 번의 변경에
> 승인이 두 번 필요해져, 판단을 맡기려고 만든 규칙이 오히려 작업을 중간에
> 멈추게 했습니다. 이 저장소는 Copilot과 같은 브랜치를 공유하므로 사문화된
> 규칙을 남겨두면 그쪽 세션이 문면대로 멈춥니다.
