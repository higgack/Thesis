# AGENT GUIDE

이 문서는 이 프로젝트에서 에이전트가 사용자와 소통할 때 지켜야 하는 기본 규칙을 정의합니다.

**우선순위**: 승인·출력·작업완료에 관한 규칙은 `AGENTS.md`가 최종 기준입니다.
이 문서와 어긋나면 `AGENTS.md`를 따르세요. 지시문을 감사할 때 두 파일을 같이 봅니다.

예외는 **§4의 답변 순서 하나뿐**입니다("기본"이지 "항상"이 아니므로, 길이와 각
항목 안의 순서는 `AGENTS.md`의 Token-lean「결론 먼저」가 계속 지배합니다).
**어디에 적용되는지도 `AGENTS.md`를 따릅니다** — 변경 **제안·설명**에는 적용하고,
상태 보고·넘겨주는 명령·한 줄 사실 답변·diff 요약에는 적용하지 않습니다. 이 범위가
없으면 예/아니오 한 줄에도 다섯 개 소제목이 붙습니다.
**§3의 "요청했을 때만 코드"는 예외가 아닙니다** — VM 명령, 실패한 스크립트의
실제 출력, **보고하는 diff**는 요청이 없어도 보여줍니다(`AGENTS.md`의
「VM ops」와 「완료 보고 전 검증」이 요구함). diff는 **보고**하는 것이지 편집
허락을 구하는 게 아닙니다.

## 1) 언어 규칙
- 모든 답변은 한국어로 작성합니다.

## 2) 대상 독자 규칙
- 설명 대상은 비개발자입니다.
- 어려운 개발 용어는 쉬운 한국어로 풀어서 설명합니다.

## 3) 설명 우선순위 규칙
- 코드 자체 설명보다 비즈니스 목적, 현재 상태, 문제점, 개선방안, 예상 효과를 우선 설명합니다.
- 사용자가 코드를 요청한 경우에만 코드를 제시합니다.

## 4) 답변 형식 규칙
- 답변은 아래 순서를 기본으로 사용합니다.
  1. 비즈니스 목적
  2. 현재 상태
  3. 문제점
  4. 개선방안
  5. 예상 효과

## 5) 변경 작업 규칙
- **보통의** 파일 수정은 사전 승인 없이 진행합니다. **트리거 단어가 오기 전까지만**
  디스크에 쌓아둡니다 — 트리거가 오면 커밋→푸시→배포 파이프라인을 끝까지 진행하며,
  이 줄을 근거로 중간에 멈추지 않습니다.
- **편집이라도 먼저 물어야 하는 예외**가 있습니다 — `AGENTS.md`「Standing rules」의
  *"'Ordinary' excludes these"* 목록이 원본입니다: Cost defaults 변경 · MCP 서버에
  쓰기/변경 도구 추가 · MCP 서버에 Chroma 추가(RAM 트레이드오프 재논의) ·
  `_HELP_TEXT`에서 **살아있는** 명령 삭제 · `## Backlog` 항목 착수 · 아래의 되돌릴
  수 없는 작업. 닫힌 목록이 아닙니다 — "without asking / explicit request /
  explicit approval"이라고 적힌 섹션은 전부 같습니다.
- **커밋은 승인을 "구하는" 게 아니라 트리거 단어로 "감지"합니다.** 「푸시할까요?」라고
  묻지 마세요 — `AGENTS.md`의 「📦 Batch/accumulate」가 그걸 금지합니다. 커밋 판단
  기준은 「⛔ Commit/push gate」가 유일한 출처입니다.
- 되돌릴 수 없는 작업은 **그 줄을 쓰는 자리에서** 확인합니다: 데이터 삭제,
  `_ENT_STOP`·`_ENTITY_ALIASES` 추가(엣지 대량 삭제·병합), 위키 토픽 삭제,
  `.env` 값 변경(실행은 사용자가 하지만 현재 값을 먼저 확인하고 영향에 합의한 뒤
  제안합니다). 이 목록은 예시이고 원본은 `AGENTS.md`「Standing rules」입니다 —
  목록에 없어도 되돌릴 수 없으면 먼저 묻습니다. **force-push는 여기 없습니다** —
  확인받고 하는 게 아니라 무조건 금지입니다(`AGENTS.md`「Branch / push policy」
  첫 항목).

> 2026-09-06 개정. 원래 문구는 "수정 전에 계획을 먼저 설명하고 사용자
> 승인을 받은 뒤 진행합니다"였습니다. 커밋 게이트와 겹치면 한 번의 변경에
> 승인이 두 번 필요해져, 판단을 맡기려고 만든 규칙이 오히려 작업을 중간에
> 멈추게 했습니다. 이 저장소는 Copilot과 같은 브랜치를 공유하므로 사문화된
> 규칙을 남겨두면 그쪽 세션이 문면대로 멈춥니다.
