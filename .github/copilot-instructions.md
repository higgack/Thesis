⛔ `main` is not this repository's project — the codebase lives only on the
branch `claude/personal-rag-knowledge-base-sLSvV`. See `AGENTS.md` at the
repo root for how to switch and what to read there.
